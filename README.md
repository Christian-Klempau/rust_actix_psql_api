Small project to showcase a Rust REST API, connecting to a PSQL database, reading and writing data. Especially good for high performance needs. Response times: 3-5ms 

## Build:

- `cargo build`

## Run:

- `cargo r -r`